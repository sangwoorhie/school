{
  "scripts": {
    "build": "cd school && npm install && npm run build"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@nestjs/platform-express": "^11.1.1",
    "aws-serverless-express": "^3.4.0",
    "express": "^5.1.0",
    "googleapis": "^149.0.0"
  }
}
